{
  "name": "server",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "dev": "parcel src/server.ts",
    "build": "webpack --config webpack.config.js  --watch --progress --mode=development"
  },
  "devDependencies": {
    "path-browserify": "^1.0.1",
    "process": "^0.11.10",
    "stream-http": "^3.2.0"
  }
}
